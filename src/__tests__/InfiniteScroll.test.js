// import { render, fireEvent } from '@testing-library/react';
// import InfiniteScrollComponent from './InfiniteScrollComponent';

// describe('InfiniteScrollComponent', () => {
//   test('loads more items on scroll', () => {
//     const { getByTestId } = render(<InfiniteScrollComponent />);
//     // const scrollContainer = getByTestId('scroll-container');
    
//     // fireEvent.scroll(scrollContainer, { target: { scrollY: 500 } });
    
//     // Expect function to have been called to load more articles
//   });
// }); 
